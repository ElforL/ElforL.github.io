import 'package:laith_shono/models/Project.dart';

class ElforConfiguration {
  // final bool _show404;
  // final bool? _loggedIn;
  // final Project? _selectedProject;

  // ElforConfiguration(
  //   this._show404,
  //   this._loggedIn,
  //   this._selectedProject,
  // );

  // factory ElforConfiguration.loading() => ElforConfiguration(false, null, null);
  // factory ElforConfiguration.home() => ElforConfiguration(false, true, null);
  // factory ElforConfiguration.project(Project project) => ElforConfiguration(false, true, project);
  // factory ElforConfiguration.unknown() => ElforConfiguration(true, null, null);

  // bool get isUnknown => _show404;
  // bool get isLoading => !_show404 && (_loggedIn == null || !_loggedIn!);
  // bool get isHome => !_show404 && _loggedIn == true;
  // bool get isProjectPage => !_show404 && _loggedIn == true && _selectedProject != null;
}
